class Solution:
    def findDelayedArrivalTime(self, arrivalTime: int, delayedTime: int) -> int:
        
        Time = arrivalTime + delayedTime
        
        if(Time<24):
            return Time
        if(Time>=24):
            return Time-24
