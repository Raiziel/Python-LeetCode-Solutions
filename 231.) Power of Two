class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        
        n = n/2
        if n == 1:
            return True
        elif n > 2:
            return isPowerOfTwo(n)
        else:
            return False
